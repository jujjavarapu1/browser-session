development:
  adapter: postgresql
  encoding: utf8
  database: beautiful_university
  username: root
  password:
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  database: beautiful_university
  username: root

# NOTE:
# See environments/production.rb for the Overriding config
# Configuration for staging and production environments is read from database.yml.erb,
# this ensures that sensitive information such as passwords are not checked in into the codebase.
